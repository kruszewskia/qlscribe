#!/bin/sh
set -e

if [ "$1" = "remove" -o "$1" = "upgrade" ] ; then
	PID=`pidof lscribed`
	if [ "X$PID" != "X" ] ; then
		kill $PID
	fi
fi

if [ "$1" = "remove" ] ; then
	/etc/init.d/dbus reload
fi

#DEBHELPER#

exit 0


